# adding a separate docker compose file for each profile to handle running of configserver + services for that profile.
services:
  rabbit:                               # define a service named 'rabbit' to perform a health check on rabbitmq
    image: rabbitmq:4-management        # container since it is a dependency for configserver container. This
    hostname: rabbitmq                  # also runs in the same network as the rest to be accessible to each other
    ports:
      - 5672:5672
      - 15672:15672
    healthcheck:
      test: rabbitmq-diagnostics check_port_connectivity
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 5s
    networks:
     - demobank

  configserver:                         # adding configserver as a service with the same name to run in the
    image: "demobank/configserver:s6"   # network 'demobank' to allow the containers to communicate with each other
    container_name: "configserver-ms"   # by name since each container has its own isolated namespace/loopback
    ports:                              # for 'localhost'. If accounts container is running outside a network,
      - "8071:8071"                     # it won't be able to find configserver at localhost:8071 since accounts
    deploy:                             # container has its own 'localhost' and nothing is listening on 8071.
      resources:
        limits:
          memory: 700m
    depends_on:               # add a dependency on configserver to start only when rabbitmq container is healthy
      rabbit:
        condition: service_healthy
    healthcheck:              # add a health check with instruction on what to test for. This one checks for readiness of configserver container
      test: "curl --fail --silent localhost:8071/actuator/health/readiness |grep UP || exit 1"
      interval: 10s           # instruct the health check to start only 10s after the container starts, to perform
      retries: 10             # up to 10 retries, with an interval of 10s between each. If the check takes longer
      timeout: 5s             # than 5s from the time it started, it is considered to have failed.
      start_period: 10s
    networks:
      - demobank

  accounts:
    image: "demobank/accounts:s6"
    container_name: "accounts-ms"
    ports:
      - "8080:8080"
    deploy:
      resources:
        limits:
          memory: 700m
    depends_on:                       # add a dependency on configserver service to be healthy prior to starting
      configserver:
        condition: service_healthy
    networks:
      - demobank
    environment:                       # defining environment variables to allow comms with configserver as mentioned above
      SPRING_APPLICATION_NAME: accounts
      SPRING_CONFIG_IMPORT: configserver:http://configserver:8071/
      SPRING_PROFILES_ACTIVE: default

  cards:
    image: "demobank/cards:s6"
    container_name: "cards-ms"
    ports:
      - "9000:9000"
    deploy:
      resources:
        limits:
          memory: 700m
    depends_on:
      configserver:
        condition: service_healthy
    networks:
      - demobank
    environment:
      SPRING_APPLICATION_NAME: cards
      SPRING_CONFIG_IMPORT: configserver:http://configserver:8071/
      SPRING_PROFILES_ACTIVE: default

  loans:
    image: "demobank/loans:s6"
    container_name: "loans-ms"
    ports:
      - "8090:8090"
    deploy:
      resources:
        limits:
          memory: 700m
    depends_on:
      configserver:
        condition: service_healthy
    networks:
      - demobank
    environment:
      SPRING_APPLICATION_NAME: loans
      SPRING_CONFIG_IMPORT: configserver:http://configserver:8071/
      SPRING_PROFILES_ACTIVE: default

networks:
  demobank:
    driver: "bridge"